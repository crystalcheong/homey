export const listingTypes: string[] = ["rent", "sale"];

// export const getCapitalizedStr = (s: string) => (s && s[0].toUpperCase() + s.slice(1)) || ""

// export const delay = (timeMs: number) => new Promise(function (resolve) {
//   setTimeout(resolve, timeMs)
// })

export const testAccount = {
  name: "Test",
  email: "test@gmail.com",
  password: "test",
};
